#include "RudyPCH.h"

// config in visual studio:
// in the .CPP, set - procompiled header to Create;

// in the project settings, set - use precompiled header to  "use"  the included .h
//
// the content in PCH header should be fixed to some extent,  to avoid recompilation;